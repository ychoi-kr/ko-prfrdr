true cases:
  - 어드벤처(adventure)은 
  - 이 알고리즘을 배치 정규화(Batch Normalization)이라고 한다.
  - 영수(레스토랑 사장)이 가게 문을 열었습니다.
  - 신경망에서 각 연산 단위(비유적으로 뉴런이라 부른다)는 층(layer)를 이뤄 다른 뉴런과 연결된다.

false cases:
  - 1바이트 초과 시 오류가 발생한다.
  - 두 번째 1은 채널 수다(여기서는 흑백 이미지이므로 1이다).
  - "공유한 내용(라우트 프리픽스(route prefix)는 모두 알려야 한다)과 모노레포로"
  - 배치 크기(batch_size)이고 
